n=0
class qwerty:
    def __init__(self):
        return self
    def get(hh):
        return bytearray.fromhex(hh).decode()
    def set(hh):
        return bytearray.fromhex(hh).decode()
    def make(hh):
        return bytearray.fromhex(hh).decode()
    def redo(hh):
        return bytearray.fromhex(hh).decode()
    def undo(hh):
        return bytearray.fromhex(hh).decode()
    def zim(hh):
        return bytearray.fromhex(hh).decode()
    def take(hh):
        return bytearray.fromhex(hh).decode()
    def tape(hh):
        return bytearray.fromhex(hh).decode()
    def let(hh):
        return bytearray.fromhex(hh).decode()

